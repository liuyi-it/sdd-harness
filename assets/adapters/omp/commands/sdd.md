---
description: 用 sdd-harness 处理自然语言需求
---

使用 sdd-harness skill 处理下面的自然语言需求；主 Agent 负责最终检查和审查。需要显式控制工作流时，使用 `/sdd.init`、`/sdd.status`、`/sdd.plan`、`/sdd.verify`、`/sdd.review` 或 `/sdd.archive`。

$ARGUMENTS
