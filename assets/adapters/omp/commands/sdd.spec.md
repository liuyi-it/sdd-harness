---
description: 统一澄清需求与技术方案并生成唯一规格
---

使用 sdd-spec skill 处理 `$ARGUMENTS`：新需求进入 `sdd spec`，修订已有 change 时进入 `sdd change` 后的统一规格阶段。先完成必要澄清，再回传同时包含需求规格和技术设计的结构化结果；不得在规格完成前修改业务文件或直接编辑 `.sdd/`。

$ARGUMENTS
