你正在维护仓库 `liuyi-it/sdd-harness`。

当前基线为：

```text
2077668088ee1d883319d94d0bef3392275c2c38
```

你的任务是将该仓库修复到可正式验收的 Release Candidate 状态。

## 核心要求

不要只修复某一个报错，也不要在第一个测试通过后停止。你必须：

1. 阅读整个仓库的架构、状态机、CLI、MCP、Schema、测试和发布脚本。
2. 将 `sdd auto` 视为产品主入口。
3. 保留手动命令模式。
4. 不增加 `loop` 命令。
5. Node.js 只要求支持 22 及以上。
6. 不发布 npm，但必须完成 `npm pack` 和本地安装 smoke test。
7. 不要求用户逐步输入；auto 应自动推进，只有澄清、Agent handoff 和人工门禁可以暂停。
8. 不得删除或弱化现有测试来让构建通过。
9. 不得使用 `.skip`、静默 catch 或 warning 替代错误处理。
10. 每次修复后运行相关测试；所有实现完成后运行完整门禁。

## 必须关闭的阻断问题

### 1. Agent handoff 文件安全

`build complete` 不得信任 Agent 自报的 modifiedFiles。

实现：

- build next 保存真实 Git baseline；
- build complete 计算真实 Git delta；
- 校验 added/modified/deleted；
- 检测未申报修改；
- 检测 allowedFiles/expectedNewFiles/forbiddenFiles 越权；
- worktree 模式检查业务 worktree；
- 最终 artifact 使用真实 delta；
- 增加 E_UNDECLARED_FILE_CHANGE；
- 增加完整安全测试。

### 2. MCP 工具分页和结果真实性

实现完整 tools/list cursor 分页。

确保：

- availableTools 是真实 MCP tool name；
- supportedIntents 从工具推导；
- fallback 结果保持 provider/degraded/reason/confidence；
- queryImpact 不得把 fallback 包装成精确 MCP；
- fallback items 不得丢失；
- capability artifact 反映真实能力。

### 3. MCP 生命周期

- 删除 spawn 进程级 timeout；
- timeout 只作用于单个 JSON-RPC request；
- 处理 child exit/error；
- 清理 pending request；
- query 前检查 session；
- stop 清理所有 timer；
- 不产生孤儿进程；
- MCP 运行 60 秒后仍可查询；
- 增加异常退出测试；
- 增加 Windows 启动兼容。

### 4. MCP 可用摘要

MCP 成功时不得生成空壳 summary。

始终先生成基础文件扫描摘要，再使用 MCP 增强：

- packages；
- modules；
- architecture；
- entrypoints；
- symbols；
- routes；
- tests；
- build/test commands。

MCP 空结果不能删除基础扫描结果。

## 必须完成的状态机修复

- restart waiting run 时失效旧 handoff；
- 不复用旧 resultFile；
- stop 后不得留下可提交到 ABORTED run 的 pending handoff；
- resume historical waiting run 时重建 handoff，不能静默丢失；
- 为 loop metadata 增加短锁或 CAS；
- 实现 maxRetriesPerStep、maxRepeatedFailures 和 no-progress detection；
- 保证 run、event、state 和 pendingAgentTask 一致；
- 不重新引入 FileLock 嵌套自锁。

## 必须完成的 Schema 和迁移

将 WorkflowState schema 升级到 `1.4.0`。

支持：

```text
1.0.0 → 1.4.0
1.2.0 → 1.4.0
旧 1.3.0 → 1.4.0
```

迁移必须：

- 备份；
- 可重复执行；
- 从 waiting 重建 pendingAgentTask；
- 无法恢复时明确 FAILED；
- 生成迁移报告；
- 增加 fixture 测试。

## 必须完成的 MCP 契约统一

统一 McpQueryInput：

```ts
interface McpQueryInput {
  intent: McpQueryIntent;
  query: string;
  root: string;
  changeId?: string;
  hint?: QueryHint;
}
```

禁止通过类型断言丢弃 query。

统一 capabilities：

```ts
interface CanonicalMcpCapabilities {
  provider: string;
  version: string;
  commit: string;
  availableTools: string[];
  supportedIntents: McpQueryIntent[];
  supportsIndex: boolean;
  supportsGraphQuery: boolean;
}
```

按 tool/intent 建立独立 decoder。处理：

- structuredContent；
- content text；
- isError；
- pagination；
- 空结果；
- 未知响应。

## 必须完成的 V2 结果持久化

解析结果同时保留：

```ts
{
  (canonicalResult, validatedOriginalArtifact);
}
```

要求：

- task-results.json 保存 canonical；
- run artifact 保存完整 V2；
- 保留 timestamps、mode、summary、commandEvidence、notes；
- fileDelta 使用系统检测值；
- round-trip 测试比较完整字段。

## 自动化验收

必须覆盖：

- 完整手动工作流；
- 完整 auto 工作流；
- clarification；
- build handoff；
- Agent 成功和失败；
- stop/restart/resume；
- current/historical waiting run；
- 并发 auto；
- 进程中断；
- state/backup 损坏；
- 三个旧 Schema 迁移；
- MCP 正常；
- MCP fallback；
- MCP tools/list 多页；
- MCP 运行 60 秒；
- MCP 异常退出；
- Windows MCP 启动；
- Git delta 越权和隐瞒；
- V2 无损持久化；
- 路径和命令注入。

## 执行方式

先建立问题清单和实施顺序，然后直接修改代码。不要等待人工确认。

推荐顺序：

1. 完成 Schema 1.4.0 和迁移；
2. 重构 pending handoff 和 Git baseline；
3. 修复 build complete 真实 delta；
4. 重构 MCP session 和 lifecycle；
5. 完成 tools/list 分页；
6. 统一 MCP query/capability 类型；
7. 完成 typed decoder；
8. 修复 fallback 身份和 payload；
9. 修复 loop stop/restart/resume/concurrency；
10. 补全测试；
11. 建立三平台 CI；
12. 更新 dist 和文档；
13. 执行全部门禁；
14. 根据失败结果继续修复，直到全部通过。

## 必须执行的最终命令

```bash
npm ci
npm run format:check
npm run lint
npm run typecheck
npm run validate:schemas
npm test
npm run validate:release
npm run build
git diff --exit-code
npm pack
```

对生成的 tarball 进行临时目录安装，并运行：

```bash
sdd --version
sdd --help
```

再执行至少一次真实完整流程：

```text
init
new
design
plan
build next
build complete
verify
review
archive
```

以及：

```text
auto
agent handoff
complete
resume/restart/stop
```

## 禁止行为

- 不得删除失败测试；
- 不得降低核心断言；
- 不得将 P0/P1 改成文档 TODO；
- 不得以 mock 测试替代全部真实 MCP smoke test；
- 不得把 fallback 标记为 MCP 正常；
- 不得信任 Agent 自报文件作为事实源；
- 不得留下旧 Schema 无迁移；
- 不得提交与源码不一致的 dist；
- 不得在测试未全部通过时宣布完成。

## 最终输出

完成后生成 `docs/release-gate-report.md`，包含：

- 最终 commit；
- 修改文件；
- 每个问题的修复说明；
- AC-001 至 AC-030 的状态；
- 所有实际执行命令；
- 每条命令的退出码；
- Linux、Windows、macOS CI 结果；
- MCP 正常和 fallback 证据；
- 完整工作流证据；
- 剩余风险。

只有所有发布门禁通过且不存在已知 P0/P1 时，最终结论才允许写：

```text
PASS
```

否则必须写：

```text
REQUEST_CHANGES
```

继续修复，不要在 REQUEST_CHANGES 状态停止。
