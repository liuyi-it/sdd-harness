# Root Cause Minimal Fix

修复失败时，不要只修复错误消息描述的单一路径。

1. 复现失败并记录 failure signature。
2. 找到真正负责该行为的共享函数、状态迁移或协议边界。
3. 检查所有调用者和相邻路径。
4. 优先在最靠近根因的位置修复一次。
5. 避免在多个调用点复制相同 guard。
6. 添加能在根因再次出现时失败的最小完整回归测试。
7. 不扩大到与当前 failure 无关的重构。
8. 不因减少 diff 而保留同类已知错误。
